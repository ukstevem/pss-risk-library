# embedding-proxy configuration
# Orin Ollama base URL (the GPU box; must have `ollama pull nomic-embed-text`)
EMBEDDING_ORIN_URL=http://10.0.0.74:11434
EMBEDDING_MODEL=nomic-embed-text
# Label stored on every library_vector row (lets you stage a model swap later)
EMBEDDING_MODEL_VERSION=nomic-embed-text-v1.5
# Fixes the vector(N) column in migration 040 — must match the model
EMBEDDING_DIM=768
PORT=8009
